{
  "name": "goodreads-shelf-mcp",
  "version": "1.0.1",
  "description": "MCP server for fetching and searching Goodreads shelf data via RSS",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepare": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.28.0",
    "fast-xml-parser": "^5.5.9",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.19.15",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  }
}
